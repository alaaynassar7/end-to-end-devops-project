apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "temp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "temp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "temp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
